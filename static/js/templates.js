// templates.js - small enhancements
document.addEventListener("click", function(e){
  // confirm clone (if any)
  if(e.target && e.target.matches(".template-card .btn-sm")) {
    // can add behavior if needed
  }
});
